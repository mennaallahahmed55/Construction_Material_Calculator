<Window x:Class="Construction_Material_Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Construction_Material_Calculator"
        mc:Ignorable="d"
        Title="Material Calculator" 
        Height="600" 
        Width="600"
        Background="Gainsboro">

    <Border Margin="20" Style="{StaticResource ModernStyleBorder}">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Title and Menu -->
            <Border Grid.Row="0"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,0,0,1">

                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title -->
                    <TextBlock Text="Material Catalog"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource BrushTextPrimary}"
                           VerticalAlignment="Center"/>

                    <!-- Menu -->
                    <Menu Grid.Column="1"
                      VerticalAlignment="Center"
                      Background="Transparent">
                        <MenuItem Header="File">
                            <MenuItem Header="Load" Click="MenuItem_Click_3"/>
                            <MenuItem Header="Export" Click="MenuItem_Click_2"/>
                            <Separator/>
                            <MenuItem Header="Exit" Click="MenuItem_Click_1"/>
                        </MenuItem>

                        <MenuItem Header="Help">
                            <MenuItem Header="About" Click="MenuItem_Click"/>
                        </MenuItem>

                    </Menu>
                </Grid>
            </Border>

            <!-- Stats Panel -->
            <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                    <Border Style="{StaticResource ModernStyleBorder}" Width="140" Height="110" Margin="5" Padding="10" >
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="CATALOG" FontWeight="Bold" FontSize="13" TextAlignment="Center"/>
                            <TextBlock Text="{Binding Materials.Count}" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="5,3"/>
                            <TextBlock Text="Materials" FontSize="13" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ModernStyleBorder}" Width="140" Height="110" Margin="5" Padding="10" >
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="ORDERS" FontWeight="Bold" FontSize="13" TextAlignment="Center"/>
                            <TextBlock Text="{Binding OrdersCount}" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="5,3"/>
                            <TextBlock Text="Pending: 3" FontSize="13" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ModernStyleBorder}" Width="140" Height="110" Margin="5" Padding="10" >
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="TOTAL SPENT" FontWeight="Bold" FontSize="13" TextAlignment="Center"/>
                            <TextBlock Text="{Binding TotalSpent, StringFormat=EGP {0:N0}}" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="5,3"/>
                            <TextBlock Text="This Month" FontSize="13" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </StackPanel>

            <!-- DataGrid -->
            <DataGrid Grid.Row="2"
                  x:Name="DataGrid" 
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  ItemsSource="{Binding Materials}" 
                  SelectionChanged="DataGrid_SelectionChanged"
                  Margin="20,20,20,20"
                  BorderBrush="{StaticResource BrushBorder}"
                  Background="{StaticResource BrushSurface}"
                  RowBackground="{StaticResource BrushSurface}"
                  AlternatingRowBackground="{StaticResource BrushSurfaceSecondary}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BrushBorder}"/>

            <!-- Footer with Buttons -->
            <Border Grid.Row="3" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,1,0,0">
                <StackPanel Margin="20" Orientation="Horizontal" HorizontalAlignment="Right" >
                    <Button Margin="5,0" Content="Add Material" Style="{StaticResource SuccessBtn}" Click="AddMaterial_Click"/>
                    <Button Margin="5,0" Content="Orders" Style="{StaticResource PrimaryBtn}" Click="Order_Click"/>
                    <Button Margin="5,0" Content="Calculator" Style="{StaticResource WarningBtn}" Click="Calculator_Click"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="4" Margin="20" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Total Materials: " FontWeight="Bold"/>
                <TextBlock x:Name="TotalMaterialsTextBlock" Text="{Binding Materials.Count}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>